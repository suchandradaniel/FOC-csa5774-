#include <stdio.h>

int main() {
    int limit, i;

    printf("Enter the limit of the series: ");
    scanf("%d", &limit);

    printf("Odd numbers from 1 to %d are:\n", limit);

    for (i = 1; i <= limit; i += 2) {
        printf("%d ", i);
    }

    return 0;
}
